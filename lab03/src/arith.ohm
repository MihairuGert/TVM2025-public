Arithmetic {
    Sum = NonemptyListOf<Sub, "+"> 
    Sub = NonemptyListOf<Mul, "-">
    Mul = NonemptyListOf<Div, "*">
    Div = NonemptyListOf<Atom, "/">
    Atom = 
    | "(" Sum ")" -- parenthesis 
    | "-" Sum -- unaryMin
    | number
    number = digit+ --number 
    | letter+ --variable
}